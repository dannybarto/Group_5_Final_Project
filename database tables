--Creating database tables

CREATE TABLE CLEAN_COUNT_DATA (
	INCIDENT_REPORT_ID INT NOT NULL,
	CITY VARCHAR NOT NULL,
	DIVISION_ID INT NOT NULL,
	PLACE_TYPE_DESCRIPTION VARCHAR NOT NULL,
	CLEARANCE_STATUS VARCHAR NOT NULL,
	HIGHEST_NIBRS_CODE VARCHAR NOT NULL
);

CREATE TABLE CLEAN_DATE_DATA (
	INCIDENT_REPORT_ID INT NOT NULL,
	CITY VARCHAR NOT NULL,
	DIVISION_ID INT NOT NULL,
	DATE_INCIDENT_BEGAN DATE NOT NULL,
	HIGHEST_NIBRS_CODE VARCHAR NOT NULL,
	YEAR INT NOT NULL,
	MONTH INT NOT NULL,	
	YEAR_AND_MONTH VARCHAR NOT NULL
);
	
CREATE TABLE CLEAN_LOCATION_DATA (
	CITY VARCHAR NOT NULL,
	ZIP INT NOT NULL,
	LATITUDE_PUBLIC FLOAT NOT NULL,
	LONGITUDE_PUBLIC FLOAT NOT NULL,
	DIVISION_ID INT NOT NULL,
	PLACE_TYPE_DESCRIPTION VARCHAR NOT NULL,
	HIGHEST_NIBRS_CODE VARCHAR NOT NULL
);

--DROP TABLE 